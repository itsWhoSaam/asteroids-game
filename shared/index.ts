// Placeholder while the toolchain lands — the real simulation modules
// (constants.ts, sim.ts, protocol.ts) arrive in the shared-sim task.
export const sharedPlaceholder = "asteroids-shared-sim-will-land-here";
