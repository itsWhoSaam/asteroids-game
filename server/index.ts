// Placeholder while the toolchain lands — the authoritative room server
// (fixed 60 Hz tick, WebSocket host, static client serving) arrives in
// the server task.
export const serverPlaceholder = "asteroids-room-server-will-land-here";
